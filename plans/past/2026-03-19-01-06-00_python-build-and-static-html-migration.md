---
plan_id: 2026-03-19-01-06-00_python-build-and-static-html-migration
title: Python Build and Static HTML Migration
summary: Replace notebook-centric build flow with script-driven static HTML generation including per-topic and all-topics pagination.
status: past
created_at: 2026-03-19-01-06-00
---

# Python Build and Static HTML Migration

Key: `[ ]` pending task, `[x]` completed task, `[?]` needs validation, `[-]` closed task

- [x] 1. Build script foundation and runtime configuration.
  - [x] 1.1 Create script modules to replace notebook execution.
    - [x] 1.1.1 Establish package structure for scanning, metadata, summaries, rendering, and indexing.
      - [x] 1.1.1.1 Add entrypoint `build.py` with deterministic stage ordering and logging.
  - [x] 1.2 Implement local settings bootstrap for Ollama.
    - [x] 1.2.1 On first run, prompt for endpoint, model(s), and generation defaults.
      - [x] 1.2.1.1 Persist to `settings.local.json` and avoid committing secrets/config by git policy.

- [x] 2. Implement data and summary pipeline migration.
  - [x] 2.1 Port metadata generation from notebook logic.
    - [x] 2.1.1 Preserve first-seen behavior and deterministic meme ordering.
      - [x] 2.1.1.1 Regenerate `memes.json` with stable schema and timestamps.
  - [x] 2.2 Implement modernized Ollama summarization flow.
    - [x] 2.2.1 Support configurable vision and text models with retry/backoff.
      - [x] 2.2.1.1 Skip already-summarized assets unless explicitly forced.

- [x] 3. Implement fully static page output model.
  - [x] 3.1 Generate per-meme static HTML pages directly (without notebook/Jekyll dependency).
    - [x] 3.1.1 Render metadata blocks and media embeds in deterministic template order.
      - [x] 3.1.1.1 Validate media type handling for images and videos.
  - [x] 3.2 Add paginated topic indexes and paginated all-topics homepage.
    - [x] 3.2.1 Define page size, navigation links, and stable URL naming.
      - [x] 3.2.1.1 Ensure pagination supports large topic sets and large global feeds.

- [ ] 4. Verify parity and transition readiness.
  - [ ] 4.1 Compare legacy vs new outputs on a representative sample.
    - [ ] 4.1.1 Validate file counts, metadata presence, and canonical links.
      - [ ] 4.1.1.1 Capture regression checklist and remaining migration gaps.
  - [ ] 4.2 Define deprecation path for notebooks.
    - [ ] 4.2.1 Mark notebook workflow as legacy while keeping rollback option.
      - [ ] 4.2.1.1 Remove notebook dependency from primary README runbook after sign-off.
- [x] 5. Refine homepage/category presentation and sidebar branding.
  - [x] 5.1 Move the cat logo into the sidebar include and shrink it for sidebar usage.
    - [x] 5.1.1 Remove top-of-page logo rendering from site brand include while preserving site title/disclaimer context.
      - [x] 5.1.1.1 Update sidebar/category include generation and stylesheet classes so the logo appears at the top of sidebar on homepage/category pages.
  - [x] 5.2 Remove summary excerpts from homepage and category cards.
    - [x] 5.2.1 Keep card topic and first-seen metadata while dropping summary text blocks.
      - [x] 5.2.1.1 Confirm card markup still links media to meme-specific pages.

- [x] 6. Expand meme-specific page rendering with media-first layout and rich metadata output.
  - [x] 6.1 Keep detail pages image-first and ensure image clicks open the original image asset.
    - [x] 6.1.1 Preserve existing video/non-image behavior while improving metadata section structure below media.
      - [x] 6.1.1.1 Render summary, first-seen, OCR, and extra sidecar metadata in deterministic order.
  - [x] 6.2 Render summary markdown as HTML on meme-specific pages.
    - [x] 6.2.1 Convert markdown summary text to sanitized HTML before embedding in page output.
      - [x] 6.2.1.1 Keep non-markdown metadata escaped to prevent HTML/script injection.

- [x] 7. Validate and document the presentation updates.
  - [x] 7.1 Regenerate static outputs after renderer/template updates.
    - [x] 7.1.1 Verify homepage, category page, and meme-detail HTML for new sidebar/logo placement, card-content changes, and markdown-rendered summaries.
      - [x] 7.1.1.1 Spot-check at least one markdown-heavy summary page to confirm headings/lists/emphasis render as HTML.
  - [x] 7.2 Update relevant docs to reflect new rendering behavior.
    - [x] 7.2.1 Align README guidance with sidebar branding placement and meme-detail summary rendering behavior.
      - [x] 7.2.1.1 Confirm no policy/index documentation drift from plan edits.
- [x] 8. Improve card metadata readability with relative posting time.
  - [x] 8.1 Replace separate topic/first-seen lines with one inline metadata row on cards.
    - [x] 8.1.1 Render card metadata as: `<b>Topic:</b> [topic link] <b>Posted:</b> [relative time]`.
      - [x] 8.1.1.1 Show exact ISO timestamp on hover using a `title` tooltip on the posted-time element.
  - [x] 8.2 Validate generated homepage/category cards after renderer updates.
    - [x] 8.2.1 Rebuild static output and verify one-line metadata row appears on both homepage and category cards.
      - [x] 8.2.1.1 Confirm tooltip exposes full timestamp while visible text shows human-readable relative age.
- [x] 9. Add sidebar Freshest Memes section and rename all-categories heading.
  - [x] 9.1 Build a Freshest Memes list above category navigation.
    - [x] 9.1.1 Include only categories with at least one new meme in the new-window calculation.
      - [x] 9.1.1.1 Sort Freshest Memes by new-count descending (stable tie-break by category name).
  - [x] 9.2 Keep full category listing below Freshest Memes with updated section title.
    - [x] 9.2.1 Rename current Meme Categories heading to All Categories.
      - [x] 9.2.1.1 Preserve existing category counts, new badges, and mobile collapse behavior for the full list.
  - [x] 9.3 Regenerate and verify sidebar output.
    - [x] 9.3.1 Rebuild static output and verify section order: Freshest Memes first, All Categories second.
      - [x] 9.3.1.1 Confirm Freshest Memes shows only categories with new memes and correct descending ordering.
- [x] 10. Align Ollama summary requests with chat-style multimodal payloads and validate one-image inference.
  - [x] 10.1 Switch summary endpoint normalization from generate-style URLs to chat-style URLs.
    - [x] 10.1.1 Preserve support for user-provided hostnames and fully-qualified Ollama URLs.
      - [x] 10.1.1.1 Avoid breaking existing local settings values that currently point at `/api/generate`.
  - [x] 10.2 Send summary requests as chat multimodal messages with base64 image attachments.
    - [x] 10.2.1 Parse `message.content` from Ollama chat responses and keep retry/timeout handling intact.
      - [x] 10.2.1.1 Raise a clear error if the chat response is structurally valid but empty.
  - [x] 10.3 Run one direct image-summary test against the configured Ollama host.
    - [x] 10.3.1 Report the exact request target/model and whether a summary is returned for one sample image.
      - [x] 10.3.1.1 Capture the concrete failure mode if inference still does not complete.
- [x] 11. Compare alternate Ollama model behavior on the same host.
  - [x] 11.1 Run a tiny chat probe against `gemma3:27b-it-q8_0` using the same Ollama host and `/api/chat` path.
    - [x] 11.1.1 Record whether text-only inference returns or times out.
      - [x] 11.1.1.1 Use the result to distinguish model-specific failure from host/runtime failure.
  - [x] 11.2 Run one image-summary probe with `gemma3:27b-it-q8_0` if the text-only probe is viable.
    - [x] 11.2.1 Record whether a summary is returned for one sample image.
      - [x] 11.2.1.1 Capture the concrete failure mode if inference still does not complete.
- [x] 12. Switch default summary model to gemma3 and reorder meme-page artifacts.
  - [x] 12.1 Update the script defaults and local settings to prefer `gemma3:27b-it-q8_0` summaries.
    - [x] 12.1.1 Use a suffix consistent with existing gemma3 summary artifacts where possible.
      - [x] 12.1.1.1 Keep existing Ollama endpoint behavior intact by supporting both `/api/chat` and `/api/generate`.
  - [x] 12.2 Reorder meme-page metadata blocks.
    - [x] 12.2.1 Always render `First Seen` first.
      - [x] 12.2.1.1 Render remaining summary/OCR/extra artifacts by sidecar recency descending with deterministic tie-breaks.
  - [x] 12.3 Regenerate and verify one meme page against the new ordering/model preference.
    - [x] 12.3.1 Confirm the selected summary now prefers gemma3 when available.
      - [x] 12.3.1.1 Confirm the meme page shows `First Seen` first and newer artifacts above older ones.
- [x] 13. Migrate the legacy dual-analysis AI workflow into the Python build pipeline.
  - [x] 13.1 Preserve the two legacy AI analysis types from `ai.ipynb`.
    - [x] 13.1.1 Keep the short accessibility/search prompt for simple image description artifacts.
      - [x] 13.1.1.1 Keep the detailed semiotic/philosophical prompt as a separate detailed-analysis artifact type.
  - [x] 13.2 Establish a filename-safe artifact naming convention that encodes model and analysis type.
    - [x] 13.2.1 Use filenames shaped like `<asset>.<sanitized_model>.<analysis_type>.txt` where illegal filename characters are normalized.
      - [x] 13.2.1.1 Keep backward-compatible discovery for existing legacy gemma/llama artifact files during migration.
  - [x] 13.3 Make model and prompt configurable per analysis type.
    - [x] 13.3.1 Support separate `simple` and `detailed` AI settings while using gemma for both by default.
      - [x] 13.3.1.1 Keep it easy to swap model and prompt independently for each analysis type later.
  - [x] 13.4 Use the most recent simple-analysis artifact as the image alt-text/search description source.
    - [x] 13.4.1 Render all discovered AI artifacts on meme pages with deterministic ordering.
      - [x] 13.4.1.1 Keep `First Seen` pinned first, then render remaining artifacts newest-first with stable tie-breaks.
  - [x] 13.5 Run a one-image validation on the migrated workflow.
    - [x] 13.5.1 Confirm simple and detailed artifacts can both be generated/read and that the meme page chooses the latest simple artifact for alt text.
      - [x] 13.5.1.1 Confirm legacy artifacts remain visible during the transition.



- [x] 14. Restore markdown artifact rendering and Freshest sidebar sections after dual-analysis migration.
  - [x] 14.1 Ensure artifact markdown renders to HTML even when optional markdown package is unavailable.
    - [x] 14.1.1 Add fallback inline markdown formatting support for bold/italics/code/links in renderer fallback path.
      - [x] 14.1.1.1 Verify meme detail pages show formatted HTML instead of raw markdown markers.
  - [x] 14.2 Reintroduce sidebar split sections with ordering requirements.
    - [x] 14.2.1 Render `Freshest Memes` above category navigation using only categories with `new_count > 0` sorted desc by new-count then name.
      - [x] 14.2.1.1 Rename lower section heading to All Categories while preserving full-list count/new-badge/collapse behavior.
  - [x] 14.3 Rebuild and validate output for both fixes.
    - [x] 14.3.1 Verify generated _includes/categories.html has two sections in the right order and labels.
      - [x] 14.3.1.1 Verify at least one meme page renders AI artifact markdown as HTML.

- [x] 15. Add category-aware prompt context and resilient missing-artifact retry behavior.
  - [x] 15.1 Include category-name perspective guidance in both simple and detailed prompt flows.
    - [x] 15.1.1 Pass per-asset category context into request prompts so analyses are framed from the saved category perspective.
      - [x] 15.1.1.1 Keep prompt/model settings independently configurable per analysis type.
  - [x] 15.2 Make AI artifact generation failure-tolerant at per-artifact granularity.
    - [x] 15.2.1 Continue build execution when one artifact call fails, while logging failures clearly.
      - [x] 15.2.1.1 Ensure failed/missing artifacts are retried automatically on subsequent runs.
  - [x] 15.3 Document and verify how to generate all missing AI artifacts.
    - [x] 15.3.1 Add README command guidance for full missing-artifact generation (--summaries on).
      - [x] 15.3.1.1 Run a validation demonstrating failure handling does not abort the build pipeline.

- [x] 16. Add built-in build logging and diagnose AI timeout behavior on full summary runs.
  - [x] 16.1 Add script-level log file output for stdout/stderr to support debugging without shell redirection.
    - [x] 16.1.1 Support configurable log path while defaulting to a stable repo-local log file.
      - [x] 16.1.1.1 Ensure start/end markers and command context are written per run.
  - [x] 16.2 Diagnose timeout behavior seen during --summaries on full runs.
    - [x] 16.2.1 Reproduce a targeted detailed-analysis request and confirm concrete failure mode/root cause.
      - [x] 16.2.1.1 Document actionable setting guidance to avoid recurrent timeout stalls.
- [x] 17. Add phased summary backfill workflow with newest-first ordering and progress ETA.
  - [x] 17.1 Process simple-description artifacts first across all image posts sorted by first-seen descending.
    - [x] 17.1.1 Print missing-count baseline before phase execution.
      - [x] 17.1.1.1 Emit per-item progress lines with elapsed time and estimated remaining time.
  - [x] 17.2 Run detailed-analysis phase only after the basic phase completes.
    - [x] 17.2.1 Recompute missing detailed count and print phase baseline before execution.
      - [x] 17.2.1.1 Keep failure handling per artifact so remaining missing files auto-retry next run.
  - [x] 17.3 Add optional post-build Jekyll rebuild stage and document usage.
    - [x] 17.3.1 Expose CLI mode to auto/on/off Jekyll execution and log skip/failure conditions.
      - [x] 17.3.1.1 Update README commands and behavior notes for phased summaries and Jekyll stage.
- [x] 18. Make --max-topics limit AI scope only while keeping full-site rebuild outputs intact.
  - [x] 18.1 Collect all topics for rendering/categorization/homepage generation regardless of max-topics value.
    - [x] 18.1.1 Apply max-topics only to the AI summary queue selection.
      - [x] 18.1.1.1 Emit explicit log line showing scoped AI summary counts when max-topics is set.
  - [x] 18.2 Update CLI/docs text to reflect the new max-topics semantics.
    - [x] 18.2.1 Validate with a dry-run that full topic count is preserved while summaries are scoped.
      - [x] 18.2.1.1 Keep post-build Jekyll/site stages operating on the full dataset.
- [x] 19. Ignore runtime/build log files from git tracking.
  - [x] 19.1 Add a global `*.log` ignore rule to `.gitignore`.
    - [x] 19.1.1 Verify existing build log files no longer appear in `git status`.
- [x] 20. Make terminal-to-log output fully immediate for all emitted output.
  - [x] 20.1 Flush tee output on every write event so log file stays live-updated.
    - [x] 20.1.1 Add Unicode-safe console fallback to avoid aborting on non-CP1252 paths while keeping UTF-8 logs intact.
      - [x] 20.1.1.1 Validate dry-run no longer crashes and completes with live log writes.
  - [x] 20.2 Route Jekyll subprocess output through the same tee stream.
    - [x] 20.2.1 Update README logging section to state immediate flush behavior.
- [-] 21. Automatically run AI artifact generation before Jekyll rebuilds in the scheduled build flow.
  - [-] 21.1 Make the default build path auto-enable AI artifact generation when a Jekyll rebuild is requested or will run.
    - [-] 21.1.1 Preserve explicit CLI override behavior so `--summaries off` still skips AI generation and `--summaries on` still forces it.
      - [-] 21.1.1.1 Ensure new AI sidecars are generated before static page rendering and Jekyll rebuild execution so newly added memes are integrated in the same hourly run.
  - [-] 21.2 Document the scheduled-build behavior and invocation guidance.
    - [-] 21.2.1 Update README examples and behavior notes to describe automatic AI-before-Jekyll execution during hourly rebuilds.
      - [-] 21.2.1.1 Clarify how `--jekyll auto|on|off` interacts with `--summaries auto|on|off`.
  - [-] 21.3 Verify the new build-flow decision path.
    - [-] 21.3.1 Run a targeted validation covering auto, forced-off, and dry-run behavior.
      - [-] 21.3.1.1 Confirm the logging output makes it clear when AI generation was auto-enabled because Jekyll will run.
- [x] 22. Revert automatic AI execution from hourly non-interactive builds until backlog catch-up is complete.
  - [x] 22.1 Restore `build.py --non-interactive` behavior to build pages/catalog without auto-enabling AI analysis.
    - [x] 22.1.1 Keep explicit AI runs available via `--summaries on` for manual catch-up windows.
      - [x] 22.1.1.1 Ensure default hourly runs avoid race-condition risk from overlapping long AI jobs.
  - [x] 22.2 Update README guidance back to manual AI generation workflow.
    - [x] 22.2.1 Document that AI catch-up should be executed explicitly before re-enabling any scheduled AI-included build cadence.
      - [x] 22.2.1.1 Preserve existing Jekyll option documentation without implying automatic AI execution.
  - [x] 22.3 Verify reverted control flow.
    - [x] 22.3.1 Confirm non-interactive build logs no auto-enable messaging and no AI job lines when AI defaults are disabled.
      - [x] 22.3.1.1 Confirm explicit `--summaries on` path remains available for manual runs.
